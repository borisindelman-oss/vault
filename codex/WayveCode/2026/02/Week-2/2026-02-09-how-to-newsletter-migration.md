# 2026-02-09 — How-to → newsletter migration

## Topic
Convert legacy `how_to` documentation pages into `newsletter` pages across the WayveCode vault.

## What changed
- Migrated all files from `codex/WayveCode/how_to/` to `codex/WayveCode/newsletters/`.
- Renamed files from `how_to_[topic].md` to `newsletter_[topic].md`.
- Renamed and updated the index page to `newsletter_index.md`.
- Updated markdown links and wiki links pointing to old `how_to` paths.
- Standardized newsletter headings and index wording to “newsletter/issue” language.

## Result
- Old `how_to` directory removed.
- Newsletter set now lives under `~/git/vault/codex/WayveCode/newsletters/` with working internal links.
